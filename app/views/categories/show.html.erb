<section class="category-banner" style="background: linear-gradient(to right, rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.6) 60%),url('<%= image_path(@category.img_url) %>');background-size: cover; background-repeat: no-repeat; background-position: center right">
  <div class="category-info">
    <h3><%= @category.name %></h3>
    <p><%= @category.description %></p>
  </div>
</section>
<section class="product-gallery">
  <% @category.products.each do |product| %>
    <a class="product-listing" href="<%= category_product_path(@category, product) %>">
      <h3><%= product.name %></h3>
      <div class="product-image" style="background: url('<%= image_path(product.img_url) %>'); background-size: contain; background-repeat: no-repeat; background-position: center">
      </div>
      <p> $<%= product.price %> </p>
    </a>
  <% end %>
</section>
